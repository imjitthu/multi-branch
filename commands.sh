git checkout -b branch //create new branch and switch to it
git checkout branch //switch between branches
git push --set-upstream origin branch
git branch -d master //Delete the branch
git push origin --delete test
git branch -l //get the list of branches
git tag test //add tag to git
it tag -a test.release -m "test verison release" //annotated tag
git push origin tag //push tag to remote repository
git push --tags //push tags to the origin
git tag -l //get the all tags list
git tag --delete tag //delete the local tag
git push origin -d test
git show tag

only executes when changes happened in SCM
